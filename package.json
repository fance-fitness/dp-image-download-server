{
  "name": "dp-dates-server",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start:dev": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/dp-image-download-server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "@types/node": "^13.1.1",
    "body-parser": "^1.19.0",
    "cheerio": "^1.0.0-rc.3",
    "express": "^4.17.1",
    "fs-sync": "^1.0.6",
    "image-downloader": "^3.5.0",
    "path": "^0.12.7"
  }
}
